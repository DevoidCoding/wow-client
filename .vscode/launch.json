{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Attach karma electron",
            "address": "localhost",
            "port": 9333,
            // "program": "$(which npm)",
            // "args": [
            //   "test"
            // ],
            // "cwd": "${workspaceRoot}",
            "protocol": "inspector"
        }
    ]
}
